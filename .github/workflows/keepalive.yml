name: Keep-alive

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl -sf "https://twelvepions.onrender.com/health" && echo "OK" || echo "Backend sleeping, wake-up triggered"
